# The build (tsc) needs the dev dependencies even where NODE_ENV=production
# is set at build time, as it is on Render. The Dockerfile's runtime stage
# does not copy this file, so its --omit=dev still applies there.
include=dev
